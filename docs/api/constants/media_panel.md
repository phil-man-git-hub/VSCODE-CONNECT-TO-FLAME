# Constant: media_panel

**Module**: `flame`

A Flame constant identified by the name `media_panel`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.media_panel)  # -> <not available>
```

**Example usage:**

```python
# Example: target the Media Panel via API
# ui.open_panel(flame.media_panel)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
