# Constant: get_version_patch

**Module**: `flame`

A Flame constant identified by the name `get_version_patch`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.get_version_patch)  # -> <not available>
```

**Example usage:**

```python
# Example: query the patch version of Flame
# patch = flame.get_version_patch
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
