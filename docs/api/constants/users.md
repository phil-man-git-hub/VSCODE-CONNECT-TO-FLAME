# Constant: users

**Module**: `flame`

A Flame constant identified by the name `users`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.users)  # -> <not available>
```

**Example usage:**

```python
# Example: query or manage users via API
# users = admin.list(flame.users)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
