# Constant: clear_unreferenced_cache

**Module**: `flame`

A Flame constant identified by the name `clear_unreferenced_cache`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.clear_unreferenced_cache)  # -> <not available>
```

**Example usage:**

```python
# Example: trigger cache-clearing via API
# some_api.call(flame.clear_unreferenced_cache)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
