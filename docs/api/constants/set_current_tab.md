# Constant: set_current_tab

**Module**: `flame`

A Flame constant identified by the name `set_current_tab`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.set_current_tab)  # -> <not available>
```

**Example usage:**

```python
# Example: set the UI current tab
# ui.set_tab(flame.set_current_tab, 'Media')
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
