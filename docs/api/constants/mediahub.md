# Constant: mediahub

**Module**: `flame`

A Flame constant identified by the name `mediahub`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.mediahub)  # -> <not available>
```

**Example usage:**

```python
# Example: target the MediaHub via API
# ui.open_panel(flame.mediahub)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
