# Constant: duplicate

**Module**: `flame`

A Flame constant identified by the name `duplicate`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.duplicate)  # -> <not available>
```

**Example usage:**

```python
# Example: instruct a duplicate operation for a single item
# duplicate_api.call(flame.duplicate, item)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
