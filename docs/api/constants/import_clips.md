# Constant: import_clips

**Module**: `flame`

A Flame constant identified by the name `import_clips`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.import_clips)  # -> <not available>
```

**Example usage:**

```python
# Example: pass to an import API to specify clip import behavior
# importer.import(flame.import_clips, files)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
