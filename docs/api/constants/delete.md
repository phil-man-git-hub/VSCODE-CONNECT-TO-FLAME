# Constant: delete

**Module**: `flame`

A Flame constant identified by the name `delete`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.delete)  # -> <not available>
```

**Example usage:**

```python
# Example: use when specifying a delete operation or command
# cmd_api.execute(flame.delete, target)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
