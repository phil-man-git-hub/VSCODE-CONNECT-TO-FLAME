# Constant: get_version

**Module**: `flame`

A Flame constant identified by the name `get_version`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.get_version)  # -> <not available>
```

**Example usage:**

```python
# Example: query the full version tuple or string
# version = flame.get_version
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
