# Constant: find_by_wiretap_node_id

**Module**: `flame`

A Flame constant identified by the name `find_by_wiretap_node_id`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.find_by_wiretap_node_id)  # -> <not available>
```

**Example usage:**

```python
# Example: use as a search mode for finding wiretap nodes by ID
# results = search.find(flame.find_by_wiretap_node_id, node_id)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
