# Constant: set_render_option

**Module**: `flame`

A Flame constant identified by the name `set_render_option`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.set_render_option)  # -> <not available>
```

**Example usage:**

```python
# Example: pass to a render configuration API
# renderer.configure(flame.set_render_option, option)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
