# Constant: find_by_uid

**Module**: `flame`

A Flame constant identified by the name `find_by_uid`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.find_by_uid)  # -> <not available>
```

**Example usage:**

```python
# Example: pass to a search function to specify a find-by-UID mode
# results = search.find(flame.find_by_uid, uid)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
