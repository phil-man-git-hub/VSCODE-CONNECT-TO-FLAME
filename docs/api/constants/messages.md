# Constant: messages

**Module**: `flame`

A Flame constant identified by the name `messages`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.messages)  # -> <not available>
```

**Example usage:**

```python
# Example: use a messages constant to route or trigger messaging actions
# messaging.send(flame.messages, payload)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
