# Constant: duplicate_many

**Module**: `flame`

A Flame constant identified by the name `duplicate_many`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.duplicate_many)  # -> <not available>
```

**Example usage:**

```python
# Example: instructs a duplication API to duplicate many items
# duplicate_api.call(flame.duplicate_many, items)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
