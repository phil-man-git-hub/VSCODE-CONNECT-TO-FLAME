# Constant: schedule_idle_event

**Module**: `flame`

A Flame constant identified by the name `schedule_idle_event`. The exact meaning and runtime value are determined at execution time.

---

**Type:** `unknown`

**Example value:**

```python
import flame
print(flame.schedule_idle_event)  # -> <not available>
```

**Example usage:**

```python
# Example: schedule an idle callback using the constant as a key
# scheduler.add(flame.schedule_idle_event, callback)
```

**See also:**

- `docs/api/constants/` index

**Notes:**
- Values are collected at runtime and may vary between Flame versions or environments.
- Run `scripts/generate_constant_docs.py --apply` to populate actual runtime values.
