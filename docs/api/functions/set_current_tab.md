# Function: set_current_tab

**Module**: `flame`

**Signature**: `set_current_tab(...)`

set_current_tab( (str)arg1) -> bool :
    Set the given tab as the active environment.
    Keyword arguments:
    tab -- The tab to set active (MediaHub, Conform, Timeline, Effects, Batch, Tools)

